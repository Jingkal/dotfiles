# ------------------------------------------------------
# ------ Pulling configs in
# ------------------------------------------------------
install.vim:
	rsync -av -c --delete ./nvim ${HOME}/.config


# ------------------------------------------------------
# ------ Pulling configs in
# ------------------------------------------------------
pull.vim:
	rsync -av -c ${HOME}/.config/nvim .

